# 修复教师打分页面学生列表

## 问题分析

教师在 `/teacher/scoring` 页面看不到学生，原因是 `eval_class` 表的 `advisor_id`（辅导员ID）错误地指向了学生用户，而非实际的教师账号。

### 相关代码逻辑

`TeacherEvalMapper.java` 中的查询 SQL：

```sql
SELECT DISTINCT u.id, u.name, u.student_no 
FROM user u 
WHERE u.role = 0 
  AND u.class_id IN (SELECT id FROM eval_class WHERE advisor_id = #{teacherId})
```

教师能看到的学生必须满足：
1. `user.role = 0`（学生角色）
2. 学生的 `class_id` 所在班级的 `advisor_id` = 当前教师的 `id`

### 修复前数据

| user.id | user.name | eval_class.id | eval_class.class_name | eval_class.advisor_id |
|---------|-----------|---------------|----------------------|----------------------|
| 11 | 张老师 | 1 | 计算机科学与技术2401班 | 4（**错误**，是学生） |
| 12 | 李老师 | 2 | 计算机科学与技术2402班 | 5（**错误**，是学生） |
| 13 | 马辅导员 | 3 | 软件工程2401班 | 4（**错误**，是学生） |

## 修复操作

### 执行 SQL

```sql
-- 更新班级的辅导员为实际教师账号
UPDATE eval_class SET advisor_id = 11 WHERE id = 1;
UPDATE eval_class SET advisor_id = 12 WHERE id = 2;
UPDATE eval_class SET advisor_id = 13 WHERE id = 3;
```

### 修复后数据

| 班级 | advisor_id | 教师 |
|------|-----------|------|
| 计算机科学与技术2401班 | 11 | 张老师（T001） |
| 计算机科学与技术2402班 | 12 | 李老师（T002） |
| 软件工程2401班 | 13 | 马辅导员（T003） |

## 新增学生的完整步骤

如需添加新学生，需按顺序操作：

### 1. 确认教师 ID

```sql
SELECT id, name, student_no, role FROM user WHERE role = 1;
```

### 2. 创建班级（如需要）

```sql
INSERT INTO eval_class (class_name, grade, major, college, advisor_id, student_count)
VALUES ('班级名称', '年级', '专业', '学院', 教师ID, 学生人数);
```

### 3. 添加学生

```sql
INSERT INTO user (student_no, name, password, role, class_id, status)
VALUES ('学号', '姓名', '123456', 0, 班级ID, 1);
```

字段说明：

| 字段 | 值 | 说明 |
|------|-----|------|
| `student_no` | 唯一学号 | 如 202401011 |
| `name` | 学生姓名 | |
| `password` | 123456 | 默认密码 |
| `role` | 0 | 0=学生, 1=教师, 2=管理员 |
| `class_id` | 班级ID | 必须是该教师所带班级的ID |
| `status` | 1 | 0=禁用, 1=启用 |

### 4. 验证

```sql
-- 查看某位教师能看到的所有学生
SELECT u.id, u.name, u.student_no, ec.class_name 
FROM user u 
JOIN eval_class ec ON u.class_id = ec.id 
WHERE u.role = 0 AND ec.advisor_id = 教师ID;
```

## 教师账号对照

| ID | 工号 | 姓名 | 角色 | 所带班级 |
|----|------|------|------|---------|
| 11 | T001 | 张老师 | 教师 | 计算机科学与技术2401班 |
| 12 | T002 | 李老师 | 教师 | 计算机科学与技术2402班 |
| 13 | T003 | 马辅导员 | 教师 | 软件工程2401班 |
| 14 | A001 | 系统管理员 | 管理员 | - |
